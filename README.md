# Sleep Mask Programmer

Public static site for programming a Sleep Mask device over Bluetooth (Web Bluetooth).

**No firmware or hardware source lives in this repository** — only the compiled web UI.

Open the site from GitHub Pages (see repository **Settings → Pages** for the URL).
